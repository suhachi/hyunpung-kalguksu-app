# Firestore 사용자 문서 생성 - Console 가이드

## 빠른 가이드

1. [Firebase Console](https://console.firebase.google.com/project/hp-kal/firestore/data) 접속
2. **데이터** 탭 → **컬렉션 시작** (또는 `users` 선택)
3. 컬렉션 ID: `users`
4. 문서 ID: `ukjp1Q5vRHSGUhi37w8FFLeARu82` (수동 입력)
5. 필드 추가:
   - `role` (string): `owner`
   - `email` (string): `admin@hp-kal.com`
   - `name` (string): `관리자`
6. **저장** 클릭

완료!

